import { TimestampConverter } from './timestamp-converter';

describe('TimestampConverter', () => {
  it('should create an instance', () => {
    expect(new TimestampConverter()).toBeTruthy();
  });
});
